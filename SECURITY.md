# 安全策略 Security Policy

## 支持的版本

| 版本 | 支持状态 |
|---|---|
| 1.0.x | ✅ 积极维护 |

## 报告漏洞

如果你发现了安全漏洞（例如：凭据泄露、脚本注入、数据越权访问），**请不要公开 Issue**。

请通过以下方式私下报告：

- 邮件：GitHub 仓库页面的 Maintainer 联系方式
- 或直接开一个标记为 `security` 的私有 Issue（如果仓库允许）

## 安全注意事项（使用者）

- 本工具读取 B站公开评论接口，核心功能匿名可用；`SESSDATA` 仅在你需要「自动识别置顶动态」时提供
- 填写的 Cookie 只保存在本机 `~/.bili-pinned-card/config.json`，**请勿提交、截图或外传**
- 卡片渲染会下载评论中的图片到内存后合成，请勿在不可信内容上以特权身份运行
- 请勿将本工具用于违反 B站用户协议的目的（刷量、骚扰、数据滥用等）

## 修复流程

1. 收到报告后，维护者确认漏洞存在并评估影响
2. 在私有分支修复并自测
3. 发布修复版本并在 CHANGELOG 中记录
4. 漏洞细节在修复发布后公开
